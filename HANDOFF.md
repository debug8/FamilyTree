# FamilyTree — контекст і що лишилось

Короткий документ для продовження роботи в новому чаті. Розробка йде **по одній задачі за раз**, суворо за `FamilyTree_Specification.md` (задачі мають мітки T-x.y). Баги фіксимо теж **по одному**, зі зупинкою після кожного.

## Стан на зараз

**Версія: 0.9.7, у розробці.** Останній ВИПУЩЕНИЙ тег — 0.9.5. Версію піднято до 0.9.7 у
`FamilyTree.App.csproj` і `installer/FamilyTree.iss` (обидва місця мусять збігатися).

**Формат файлу: схема v3.** Міграції `v1→v2` (дати) і `v2→v3` (події особи) вбудовані, ланцюжок
працює з будь-якої старої версії. Зразки в `samples/` — поки v2; відкриються через міграцію.

### ⚠️ Найважливіше: у git НЕ закомічено нічого з 0.9.6 і 0.9.7

Усе лежить у робочій копії. Логічні коміти, за порядком робіт:

1. `v0.9.6: B-09…B-12 write-path hardening`
2. режими дерева
3. фото особи
4. життєві факти
5. `add Deceased flag and event places/notes; fix GEDCOM skipped-tag report` *(або три окремі)*
6. `preserve unknown file fields via JsonExtensionData`
7. `extract PersonEvent; schema v3 with v2→v3 migration`
8. `add project links to the About window`

Заразом: `git rm FIXES-2026-08-15.md AUDIT.md`, прибрати `FamilyTree.App\Styles\Controls-1.xaml`,
видалити зайвий `PackageReference` на `System.Text.Encoding.CodePages` у `FamilyTree.Gedcom.csproj`
(це той NU1510, що сиплеться при кожній збірці).

### Стан збірки й тестів

| Що | Де перевірено | Результат |
|---|---|---|
| `Domain`, `Storage`, `Gedcom`, `Tests` | пісочниця асистента | **596 тестів зелені**, нових попереджень немає |
| `App` (WPF) | ніде | **не збирався** — лише локально |

Єдиний фейл у пісочниці — `PhotoPathPolicyTests` із windows-шляхом `\photo.png`: ламається
через Linux, а не через зміни. Три попередження `CS8619` у `DocumentMapper` — теж давні,
не від цих правок.

**Отже перше, що робити локально: `dotnet build` + `dotnet test` на солюшні цілком.**

### Що ввійшло в 0.9.7 (десять змін, у порядку появи)

1. **`Person.Deceased`** — стан «помер, дата невідома» (дзеркало `SpouseLink.Divorced`).
2. **Звіт про пропущені теги GEDCOM** — кваліфіковані шляхи замість голих імен
   (`DEAT.PLAC` більше не ховається за `BIRT.PLAC`).
3. **Місця й нотатки подій** — `DeathPlace`, `DeathNote`, `BirthNote`, `MarriagePlace`;
   закрило пункт «Місця подій» з `IDEAS.md`.
4. **`[JsonExtensionData]`** — незнайомі поля файлу більше не губляться (новий
   `Storage/DocumentExtras.cs`, `FamilyDocument.Extras`, 5 тестів
   `Tests/Storage/UnknownFieldTransitTests.cs`); закрило однойменний пункт з `IDEAS.md`.
5. **`PersonEvent`** — події особи окремим типом, **схема v3** (нові `Domain/PersonEvent.cs`,
   `Storage/Serialization/FormatMigrationV2ToV3.cs`, 8 тестів
   `Tests/Storage/PersonEventStorageTests.cs`); закрило однойменний пункт з `IDEAS.md`.
6. **Посилання на проєкт у «Про програму»** — GitHub і `releases/latest`; новий `LinkStyle`
   у `Styles/Controls.xaml`, два ключі в обидва resx.
7. **Меню на картці особи в дереві** — ПКМ по вузлу: «Побудувати дерево від особи»,
   «Редагувати», «Додати подружжя», «Видалити». Подвійний клік (перебудова дерева) прибрано,
   панорамування переїхало на ЛІВУ кнопку. Два ключі в обидва resx.
8. **Меню на рамці подружжя** — ПКМ по рамці шлюбу: «Редагувати подружжя» (той самий діалог
   `RelationshipEditorWindow`, що й у блоці «Подружжя»). Ланцюжок як у пункту 7:
   `FamilyGraphSurface.CoupleEditRequested` → `TreeViewModel.RequestEditCouple` →
   `MainViewModel`; спільне тіло — `EditSpouseLink(link, person, spouse)`, щоб не залежати від
   виділеної особи. Один ключ в обидва resx (`Tree_Menu_EditCouple`).
9. **B-67** — повторний шлюб пари: рамка в дереві брала не той `SpouseLink` (пошук за парою без
   `IsActive`), тож тултіп показував дату давнього шлюбу, а меню з пункту 8 редагувало його ж.
   Джерело зроблено одне — `TreeViewModel.ActiveSpouseLinks(doc)`; рамка носить `LinkId`.
   Друга половина — вкладка «Особа»: `PersonCard.SpouseLinkId`, команди `EditSpouse`/`RemoveSpouse`
   беруть картку рядка й діють за Id (видалення прибирає ОДИН зв'язок), а при повторному шлюбі до
   підпису рядка додається період (`RowTitle`), щоб два рядки різнилися.
10. **B-68** — повторний шлюб пари взагалі не вводився. Три перепони: `PeriodOverlaps` вважав
    розлучення без дати вічним шлюбом; кандидат без дат накривав усю вісь часу (тепер
    попередження, а не помилка); `DirectRelativeIds` ховав колишнього як «уже пов'язаного».
    Зачіпає `Domain` (`SpouseLink`, `RelationshipValidator`, `ValidationKeys`), `MainViewModel`,
    обидва resx і `RemarriageTests` (+4 тести).
11. **B-69** — діалог «Редагувати подружжя» показував ВИМКНЕНИЙ список осіб: у темній темі білий
    (типовий шаблон `ListBox` на `IsEnabled=False` бере системний колір) і ні для чого не
    потрібний. Замість нього рядок «Шлюб з: Ім'я (роки)»; список керується `Visibility`, а
    `RefreshCandidates` у режимі редагування більше не копіює всіх осіб документа.
    Один ключ в обидва resx (`Rel_EditingWith`).
12. **B-70 (🔴 втрата даних)** — `DocumentIntegrity` дедуплікував подружні зв'язки за ключем
    «пара + дата шлюбу», тож повторний шлюб БЕЗ дат зникав при відкриванні файлу. Ключем став
    увесь вміст запису (`SpouseKey`). +2 тести в `CorruptFileTests`. Третій баг поспіль з одного
    кореня (B-67, B-68, B-70): «пара + дата» не визначає шлюб однозначно.

Перші чотири лишили схему **v2** — нові поля опційні. Підняла її лише п'ята, і саме тому,
що вона змінює СТРУКТУРУ наявних полів, а не додає нові. Шоста й далі формату не стосуються.

Шоста, сьома й восьма — **єдині зміни, що чіпають XAML**, тож у застосунку їх треба
побачити очима. Дев'ята XAML не чіпає, але перевіряється теж очима (потрібен файл із
повторним шлюбом пари).

Розбір кожної — у `CHANGELOG.md`, розділ 0.9.7.

### Що зібране, але руками в застосунку НЕ бачене

Три блоки 0.9.6 (**режими дерева**, **фото особи**, **життєві факти**) і весь 0.9.7.
Конкретний список того, що клікати, — у розділі «Як почати новий чат» наприкінці файлу.

### Баги

Актуальний список — [Issues з міткою `bug`](https://github.com/debug8/FamilyTree/issues?q=is%3Aissue+is%3Aopen+label%3Abug).
Історичний розбір із номерами рядків і доказами — `docs/archive/BUGS-2026-07-30.md` (архів).

---

> _Попередній стан (на завершення T-5.2a, 0.9.4):_ Після 0.9.3 закомічено **B-13**, **B-18**, потім **B-25**. **T-5.2a** (модель неточних дат) — **ЗАВЕРШЕНО**: 1a, 1b і вся Частина 2 (2.1–2.5 + UI-правки); збірку 0.9.4 перевірено локально. Уся видима частина працює через контрол `FamilyDateEditor`.
>
> _0.9.5:_ T-5.2 — обмін GEDCOM 5.5.1 в обидва боки; випущено, але лишилась **порція 4c**: перевірка експорту в Gramps (див. розділ «T-5.2 (GEDCOM)» нижче).

## Про застосунок

Десктопний застосунок родинного дерева, **C# / .NET 10 (LTS) / WPF, MVVM**. Зберігає, редагує й візуалізує дерево, автоматично обчислює назви спорідненості (укр + англ), багатомовний (uk/en) з живим перемиканням, має три теми (фірмова/світла/темна), формат документа — JSON `.familytree`.

## Архітектура (проєкти)

- **FamilyTree.Domain** — доменна логіка без WPF: `Person` (+`PersonEvent` — народження/смерть, +`PersonFact` — життєві факти), `ParentChildLink`, `SpouseLink`, `Entity` (Id = Guid v7), `FamilyGraph` (O(1)-навігація), валідація, ядро спорідненості `Kinship/` (finder, calculator, форматери uk/en, path explainer, `RelativeFilter` — склад режиму «Лише родичі»), розкладка `Layout/TreeLayoutEngine` (приймає необов'язкову множину `include`), генератор демо-родин `Seeding/DemoFamilyGenerator` (+`DemoFamilyOptions`/`DemoFamilyResult`).
- **FamilyTree.Storage** — `FamilyDocument`, `IFamilyStorage`/`JsonFamilyStorage` (атомарний запис + бекапи), DTO+мапери, міграції формату (`v1→v2` дати, `v2→v3` події), `DocumentIntegrity` (ремонт файлу), `FamilyMerger` (злиття), `DocumentExtras` (транзит незнайомих полів файлу).
- **FamilyTree.App** — WPF: ViewModels (CommunityToolkit.Mvvm), контроли, локалізація (resx + `{loc:Localize}`), теми (DynamicResource), DI через Generic Host, логування `Diagnostics/AppLog` (Serilog), темний заголовок вікна `Theming/TitleBarThemer`, `AppInfo` (версія/автор зі збірки), `Localization/UiLanguage` (живе перемикання мови для DatePicker), фото осіб (`Services/IPhotoStore`+`PhotoStore` — сховище, `Services/PersonPhoto` — показ).
- **FamilyTree.Tests** — xUnit + Shouldly.
- **tools/SeedGenerator** — консольний генератор файлів-зразків у `samples/`.

## Ключові домовленості

- MVVM: `[ObservableProperty]`, `[RelayCommand]`; усі сервіси/VM — `AddSingleton` у `App.xaml.cs`.
- Локалізація: `Strings.resx` (uk, база) + `Strings.en.resx`; будь-який новий текст — у ОБИДВА файли.
- Теми: кольори лише через `DynamicResource`. Три словники з ІДЕНТИЧНИМ набором ключів: `Theme.Brand.xaml`, `Theme.Light.xaml`, `Theme.Dark.xaml`. Код теми = ім'я файлу (`Theme.{Code}.xaml`), реєстрація — у `ThemeService._options`. Ключ може бути будь-яким `Brush` (у фірмовій темі частина — `LinearGradientBrush`), тому нові стилі не повинні очікувати саме `SolidColorBrush`.
- Рендеринг графа винесено в переюзабельний `FamilyGraphSurface` (DP: `NodesSource/EdgesSource/CouplesSource/BandsSource/SceneWidth/SceneHeight/Interactive`). Його використовують і вкладка «Дерево», і міні-граф у «Хто кому».
- Прив'язки контрола до власних DP — через `RelativeSource AncestorType=UserControl` (НЕ `ElementName`, НЕ самопосилання `x:Type`).
- Мова для парсингу/показу дат у WPF керується `FrameworkElement.Language` (xml:lang). Живе перемикання зроблено через `UiLanguage` (див. нижче) — не забувати чіпляти нові вікна.

## Зроблено (основне)

Каркас .NET 10, локалізація з живим оновленням, теми, домен (сутності/граф/валідація/сховище), ядро спорідненості (uk+en, лінія, стилі, пояснення шляху, колишнє подружжя, свояцтво), CRUD осіб і зв'язків, дерево (розкладка/зум/пан/re-root, бейджі, рамки шлюбу, смуги поколінь), вкладка «Хто кому» + міні-граф шляху, сортування, іконка, зразкові родини, тултіп-картка особи, підсвітка ребер/поколінь, **T-5.1 злиття/імпорт**, **T-5.3 логування + глобальні обробники помилок**, **T-5.5 демо-родина**, **T-4.5 експорт у PNG**, **T-5.4 екран налаштувань**, **T-5.6 інсталятор/публікація**, меню «Про програму», темний системний заголовок вікна.

**0.9.4:** T-5.2a — модель неточних дат (`FamilyDate`) + контрол `FamilyDateEditor`; формат файлу піднято до **v2** із вбудованою міграцією v1→v2.

**0.9.5:** T-5.2 — обмін GEDCOM 5.5.1 в обидва боки (проєкт `FamilyTree.Gedcom` + пункти меню, звіт імпорту); лишилась перевірка в Gramps (4c).

**0.9.7 (у розробці):** п'ять змін — перелік і межа «коли бампаємо схему» у розділі
«Стан на зараз» угорі.

**0.9.6:** пачка «шлях запису» — **B-09, B-10, B-08, B-11, B-12 (ч. 1–2)**, **B-66**, правки інтерфейсу, **режими дерева** («Усі» + «Лише родичі»), **фото особи** (сховище, мініатюри при експорті копії, показ на вкладці «Особа»), **життєві факти особи** (`PersonFact`: професія й проживання, `OCCU`/`RESI` в обидва боки, редактор-список, показ у картці), упорядкування документів проєкту.

**0.9.3:** вісім багів (B-65, B-01, B-16, B-17, B-14, B-15, B-03, B-04), тултіп дат у DatePicker, стан `SpouseLink.Divorced`, новий порядок рамки подружжя (колишні зліва, чинні справа, особа в центрі).

Повний журнал змін за всіма версіями — у **`CHANGELOG.md`**; ідеї на майбутнє — в **`IDEAS.md`**.

## Що лишилось

**Закрито у 0.9.4:** B-13, B-18, B-25, B-02, B-05, B-19, B-06, B-07 (крім перф-хвоста).
Деталі — у `CHANGELOG.md`.

**Пачка «шлях запису» (йде в 0.9.6) — зроблено, збірка й тести зелені:** **B-09**, **B-10**,
**B-08**, **B-11**, **B-12 (ч. 1–2)**. Деталі кожного фікса й ухвалені рішення — у `CHANGELOG.md`.
Разом із ними — правки інтерфейсу (рамка `GroupBox` і кут смуг прокрутки на темній темі, розмір
імені на вкладці «Особа» + роздільники секцій, помилка при закритті застосунку — **B-66**).

**Зібрано й протестовано (тести зелені), але В ЗАСТОСУНКУ РУКАМИ ЩЕ НЕ ПРОГАНЯЛОСЯ:**

- [x] **Режими дерева.** `Tree_Mode_Full` перейменовано на «Усі»; додано `TreeMode.RelativesOnly`
  («Лише родичі») з новим `FamilyTree.Domain/Kinship/RelativeFilter.cs` і тестами
  `FamilyTree.Tests/Domain/RelativeFilterTests.cs`.
- [x] **Фото особи.** `Services/IPhotoStore` + `PhotoStore` (копія в `%AppData%\FamilyTree\photos`),
  `Services/PersonPhoto` (показ), вибір фото в редакторі особи з **відкладеним копіюванням**,
  `Person.PhotoThumbnail` + пункт меню «Зберегти копію з фото…», фото праворуч від імені на
  вкладці «Особа» з тултіпом.
- [x] **Життєві факти особи.** `Domain/PersonFact.cs` (+`Person.Facts`), `PersonFactDto` і мапінг,
  дати фактів у `DocumentIntegrity.SanitizeDates`, доповнення при злитті у `FamilyMerger`,
  `OCCU`/`RESI` в `GedcomImporter`/`GedcomExporter`, `ViewModels/PersonFactRow.cs` + список
  у редакторі особи, показ через `PersonCardBuilder`, дев'ять ключів в обидва resx.
  **Не покрито тестом:** експорт фактів у GEDCOM (імпорт покрито трьома тестами в
  `GedcomImporterTests`). Round-trip тест — перше, що варто дописати.
- [x] **Увесь 0.9.7** — п'ять змін (перелік угорі). У редакторі особи з'явилися нотатка при
  народженні, місце й обставини смерті; у діалозі подружжя — місце шлюбу. Після `PersonEvent`
  редактор пише подію цілком (`PersonEvent.Create`), а не шість полів поокремо, тож саме його
  варто клацнути уважно. Що перевіряти — у «Як почати новий чат».

- [ ] **Перегенерувати `samples/` під схему v3** (`tools/SeedGenerator`). Файли там зараз v2 —
  вони відкриються через міграцію, тож це не терміново, але зразки в репозиторії краще
  тримати у поточному форматі. Увага: `SeedGenerator` відтворює лише `rodyna-*`; рукописні
  (`Нова родина`, `тест жжж`, `test_cousin`, `testova-rodyna`) перезбереже лише застосунок.
- [ ] **Прибрати сміття в репозиторії:** `git rm FIXES-2026-08-15.md`, `git rm AUDIT.md`
  (обидва вже враховані в `CHANGELOG.md` / `BUGS-2026-07-30.md`), видалити випадковий
  `FamilyTree.App\Styles\Controls-1.xaml`, якщо він ще там.
- [ ] **Зайвий `PackageReference`** `System.Text.Encoding.CodePages` у `FamilyTree.Gedcom.csproj` —
  пакет входить у фреймворк, збірка щоразу видає NU1510. Один рядок видалити.

- [ ] **B-12 (ч. 3)** — команда **«Відновити з резервної копії»** (вона ж S-B8; повний опис — у
  `BUGS-2026-07-30.md`, розділ «Спадок `AUDIT.md`»). Не фікс, а нова функція UI: меню, вибір слоту з `.backups`, resx. Без неї будь-яка
  відмова на завантаженні лишається безвихідною. Окрема задача.

**Відкриті задачі.** Список живе в [Issues](https://github.com/debug8/FamilyTree/issues) — і баги, і ідеї.
Дрібні дефекти зібрані в чотири парасолькові issues за шарами, щоб не засмічувати список.
Розгорнуті постановки, яких в issue немає, — в `docs/archive/`.

**Специфікація (Етап 5):**

- [x] **T-5.2a — модель неточних дат + контрол `FamilyDateEditor`.** Завершено (1a/1b/2.1–2.5 + UI-правки), збірку перевірено. Передувала T-5.2.
- [~] **T-5.2 — імпорт/експорт GEDCOM 5.5.1.** Повна постановка — у специфікації (§7, T-5.2). Частини 1–3 і 4 (4a+4b) написані; лишилась **4c — перевірка людиною**. Див. розділ «T-5.2 (GEDCOM)» нижче.

## T-5.2 — GEDCOM (код увесь написано, лишилась перевірка 4c)

Повна постановка — у `FamilyTree_Specification.md`, §7, задача **T-5.2** (профіль тегів, мапінг `INDI`/`FAM`,
кодування, звіт, критерії). Порядок: **1 → 2 → 3 → 4**.

**Що зроблено** (проєкт `FamilyTree.Gedcom`, читач, експорт, імпорт, round-trip, UI зі звітом),
**ухвалені рішення** й **граблі** — у `CHANGELOG.md`, розділ 0.9.5. Тут — лише те, що лишилось.

**ЛИШИЛОСЬ — Порція 4c (може зробити лише людина).** Пункт 2 після 0.9.7 став важливішим, ніж був:
експорт відтоді змінився у трьох місцях (`1 DEAT Y`, `DEAT` з `PLAC`/`NOTE`, `MARR` з `PLAC`), а
round-trip через власний імпортер такі помилки не ловить **за побудовою** — якщо профіль 5.5.1
зрозуміли неправильно, імпортер помиляється симетрично й тест лишається зеленим. Чужа програма —
єдиний спосіб це побачити.

1. ~~`dotnet build` + `dotnet test`~~ — **зроблено**, зелено, закомічено.
2. **Перевірка в Gramps** — критерій зі спеки. Узяти `samples/rodyna-kovalenkiv.familytree`,
   експортувати, відкрити в Gramps: чи немає помилок, чи правильні покоління, імена, дати. Непевні місця,
   за якими дивитися насамперед: `SEX U` (у строгому 5.5.1 лише M/F), кастомні `_MARNM` і `_UID`,
   `PEDI foster` для вітчима, `MARR Y`/`DIV Y`, `FAM` лише з одним `HUSB`, заглушка `SUBM`.
3. ~~Імпорт реального чужого `.ged`~~ — **зроблено у 0.9.7** і виявилося корисніше, ніж планувалося:
   саме він показав, що звіт про пропущені теги брехав, а потім визначив склад нових полів
   (`CAUS` і `DIV.PLAC` не трапилися жодного разу → окремих полів під них НЕ додано; натомість
   трапився `BIRT.NOTE`, якого в плані не було). **Сам файл у `samples/gedcom/` ще не покладено —
   варто покласти, він знадобиться знову.**
4. Позначити T-5.2 завершеною у `FamilyTree_Specification.md` і оновити цей файл.

## Важливі нюанси середовища

- **Останнє слово про складання — за локальним прогоном** (`dotnet build`, `dotnet test`):
  лише там збирається WPF-проєкт `FamilyTree.App`, а отже й уся солюшн цілком.
- **АЛЕ: пісочниця асистента .NET таки запускає** (перевірено в сесії `[JsonExtensionData]`,
  0.9.7). SDK там не стоїть, зате ставиться за ~хвилину:
  `curl -sSL https://dot.net/v1/dotnet-install.sh | bash -s -- --channel 10.0 --install-dir ~/dotnet`,
  далі `export PATH=~/dotnet:$PATH`. `Domain`, `Storage` і `Tests` — звичайні `net10.0`-бібліотеки
  й на Linux збираються; NuGet доступний. Рецепт: вивантажити вихідники цих трьох проєктів,
  **прибрати з `FamilyTree.Tests.csproj` посилання на `FamilyTree.Gedcom`** (або вивантажити і
  його) — і `dotnet test` проходить (101 тест у папці `Tests/Storage`). Тобто правки в домені й
  сховищі можна перевіряти ДО того, як вони поїдуть на машину; «наосліп» лишається тільки те,
  що чіпає WPF.
- **Локальна оболонка асистента може бути недоступна.** У сесіях 0.9.5, 0.9.6 і 0.9.7 `device_bash`
  віддавав «Workspace unavailable», тож ні `git status`, ні `grep` по репозиторію асистент
  запустити не міг — лише читати окремі файли й повертати їх назад. Схоже, це стала умова, а не
  збіг. Робочий обхід, який спрацював тричі: git-команди диктуються користувачу, а пошук по коду
  замінюється точковим читанням файлів (`grep` по вивантаженій копії теж працює).
- **Версію 0.9.5 було проставлено авансом** — до перевірки 4c. Якщо Gramps покаже проблеми в експорті
  GEDCOM, правки лягають у ту саму 0.9.5.
- **Версію піднято до 0.9.7** у `FamilyTree.App.csproj` (`Version`, `AssemblyVersion`, `FileVersion`,
  `InformationalVersion`) і `installer/FamilyTree.iss`. Обидва місця мусять збігатися, інакше
  `installer/build-installer.ps1` зупиняє збірку. Позначки «(ВИПРАВЛЕНО у 0.9.6)» у
  `BUGS-2026-07-30.md` уже стоять.
- **Стан git:** 0.9.3, 0.9.4 і «v0.9.5: T-5.2 GEDCOM import/export UI» закомічено; далі — нічого.
  Список логічних комітів — угорі, у «Стан на зараз».
- **Збірка інсталятора:** `powershell -ExecutionPolicy Bypass -File installer\build-installer.ps1`
  (публікація + ISCC + звірка версій). Деталі — `docs/build-and-installer.md`.
- Нові рядки — у ОБИДВА resx; нові діалоги — через `IDialogService` (там чіпляється і темний заголовок, і мова через `UiLanguage`).
- `Divorced` — опційне поле: `IsActive` перевіряє і `DivorceDate`, і `Divorced`; серіалізується лише коли `true` (не ламати цю сумісність).
- **`Person.Deceased` — те саме рішення, що й `Divorced`, і з тієї ж причини.** Прапорець
  позитивний («помер») НЕ зі стилю: `bool` за замовчуванням `false`, тож відсутнє поле у старих
  файлах читається як «живий». Збережений `IsAlive` дав би `false` і «поховав» усіх у кожному
  старому файлі. Не перевертати — і не ховати всередину `Person.Death`: подія зникає разом з
  останнім своїм полем, а цей стан має пережити саме повну відсутність подробиць.
- **`GedcomImporter.ConsumedTags` — кваліфіковані ШЛЯХИ, не імена тегів** (`BIRT.PLAC`, `CHIL._FREL`,
  `RESI.ADDR.CITY`). Плоский список колись мовчки ковтав `DEAT.PLAC` і `DEAT.NOTE`, бо «PLAC» і
  «NOTE» стояли в ньому заради `BIRT.PLAC` та `INDI.NOTE`. Кожен рядок мусить відповідати місцю в
  коді, яке цей тег справді читає: зайвий рядок = повернення тієї самої невидимої втрати.
  У незнайомий тег обхід (`GedcomNode.UnknownDescendantPaths`) не спускається — лічильник означає
  «скільки записів втрачено», а не «скільки рядків файлу».
- **`1 DEAT Y` / `1 MARR Y` пишуться ЛИШЕ коли підтегів немає взагалі.** Після появи місця й
  нотатки події гілку «Y» довелося зробити залежною від того, чи `BuildEvent` повернув вузол:
  «1 DEAT Y» із `2 PLAC` під ним — запис, що сам собі суперечить.
- **`Entity.Id` (UUIDv7) впорядкований лише з точністю до МІЛІСЕКУНДИ**, а всередині неї
  порядок випадковий — `Guid.CreateVersion7()` не веде там лічильника. Пачка осіб (імпорт
  GEDCOM, злиття, демо-родина) уся лягає в одну-дві мілісекунди, тож сортувати за `Id`
  заради хронології не можна: для цього є `CreatedAt` (100 нс). На унікальність це не
  впливає — її тримають 74 випадкові біти. Монотонний варіант розглядався й свідомо
  відхилений (`CHANGELOG.md`, 0.9.7) — не переробляти без реальної потреби.
- **`GedcomImporter.Import` виконується СИНХРОННО в UI-потоці** (`MainViewModel` чекає лише
  читання файлу з диска). Отже жодних затримок і важкої роботи на особу там бути не може:
  11 мс × 1 000 осіб — це заморожене вікно на 11 секунд.
- **`PersonEvent`: порожньої події не існує — замість неї `null`.** Створювати лише через
  `Create`/`With*`, ніколи через `new PersonEvent { … }` у продакшн-коді: фабрики обрізають
  пробіли й тримають цей інваріант, на який спираються і файл (порожній об'єкт не пишеться),
  і GEDCOM (`1 DEAT Y` лише коли підтегів немає).
- **`IsAlive` — умова на ДАТУ (`Death?.Date is null`), не на наявність події.** Подія лише з
  місцем не ховає особу; це робить `Deceased`. Не «спрощувати» до `Death is null`.
- **Схема піднімається лише на структурних змінах.** Нове опційне поле → схема лишається як є
  (його тримають `WhenWritingNull` і `[JsonExtensionData]`); зміна форми наявного поля → бамп
  і міграція. Так було з v1→v2 (дати) і v2→v3 (події).
- **`DocumentExtras` ключується за `Id` ГОТОВОЇ доменної сутності, не за тим, що у файлі.**
  Зв'язок без `"id"` отримує новий `Guid` прямо в `DocumentMapper.ToDomain` — саме тому там
  цикл, а не `Select`. Не повертати `Select`: поля таких зв'язків загубляться мовчки.
- **Канарейка `Own_file_produces_no_unknown_fields`.** Якщо цей тест почервонів — не «підправити
  очікування», а знайти поле DTO, яке перестало збігатися з ключем у файлі: з цього моменту воно
  возиться як «невідоме» замість того, щоб читатися.
- Після правок кровної спорідненості звіряти, що traversal іде через `GetBloodParents`, а не `GetParents`.
- Уроки з XAML (щоб не повторювати): не іменувати `ScaleTransform` у `LayoutTransform` вкладеного UserControl; не робити самопосилання `{x:Type ownControl}`.
- **`FamilyDate` — клас (record), а не структура**, тож `FamilyDate?` це НЕ `Nullable<>`: без `.Value`/`.HasValue` (перевірка `is { }`/`is not null`, порівняння прямо `==`/`!=`).
- **`PersonFact` — record без `Id`.** Отже: рівність за значенням (два однакові факти в списку не
  розрізняються, `List.Remove` прибере перший), зміна лише через `with` (у `DocumentIntegrity` саме
  тому `Facts[i] = Facts[i] with { Date = null }`), і зіставляти рядки редактора зі старими фактами
  нема за чим — список перезбирається цілком. Вид у JSON — рядок, не enum: інакше файл із
  незнайомим видом падав би на старій збірці як «пошкоджений».
- **App-VM у тест-проєкті НЕ тестуються** (Tests = `net10.0`, App = `net10.0-windows`) — перевірка логіки VM тільки прогоном застосунку.
- **Типові шаблони WPF зашивають кольори повз тему** (біла рамка в `GroupBox`, `SystemColors.ControlBrushKey` у куті `ScrollViewer`). На світлій темі непомітно, на темній — видно. Лікується власним шаблоном або перевизначенням ключа У МЕЖАХ стилю, не глобально.
- **`Person.Gender` — `required`.** У тестах `new Person { … }` без `Gender` не компілюється (CS9035);
  у сусідніх тестах беруть `Gender.Unknown`. Дрібниця, але вже двічі коштувала циклу «правка → збірка».
- **Домовленість про помилки шляху запису (з B-09):** новий сценарій відмови на записі отримує ВЛАСНИЙ
  ключ (`FileError_Write*`), а не перевикористовує читацький, якщо той у тексті згадує «прочитати» чи
  «файл не відкрито». Аргументом завжди йде цільовий файл, ніколи — temp.
- **Прапорець змін (з B-11):** `IsDirty` більше не має публічного сеттера. Забруднити документ можна
  лише через `MarkChanged()`, і `Revision` росте разом із прапорцем. `Revision` збільшується від правок,
  що проходять через `DocumentSession.MarkContentChanged` — код, який змінить `Persons`/`Links` повз
  сесію, лишиться невидимим і для цього механізму, і для зірочки в заголовку.
- **`MergePlan` одноразовий (з B-12):** між `Plan` і `Apply` документ не можна чіпати; повторний `Apply`
  того самого плану кидає `InvalidOperationException`. Для нового злиття — новий `Plan`.
- **Фото:** `Person.PhotoPath` — завжди ВІДНОСНИЙ шлях у теці даних (`PhotoPathPolicy`, B-13), файл
  копіюється під згенерованим ім'ям `{guid}{ext}`. Копіювання відкладене до `Commit()` редактора —
  «Вибрати → Скасувати» не лишає осиротілого файлу. `Person.PhotoThumbnail` — **тільки для експорту
  копії**, у звичайному збереженні не заповнюється (base64-мініатюра ~в 19 разів більша за решту
  даних особи). Показ — лише через `Services/PersonPhoto.Load` (файл → мініатюра), не напряму.
- **`DecodePixelWidth` не зменшує, а РОЗТЯГУЄ**, якщо він більший за оригінал. Тому `PersonPhoto`
  спершу читає метадані (`BitmapDecoder` + `BitmapCacheOption.None`) і виставляє його лише тоді,
  коли оригінал справді ширший.
- **Два класи в одному файлі — джерело двох підряд помилок збірки.** У `ViewModels/PersonCard.cs`
  живуть `PersonCard` і `PersonCardBuilder`; `private`-член одного з них НЕ видно другому, хоч у
  редакторі вони поруч (спіймалися `ResolvePhoto` і константа `CardPhotoWidth`). Перед тим як
  звертатися до члена «з цього ж файлу», перевіряти, чи це той самий клас.
- **Записи файлів на машину можуть тихо відкочуватися, поки відкрита Visual Studio.** Запис
  повертає успіх, **час модифікації оновлюється**, а вміст на диску лишається старий. Робочий
  порядок: записати → зачекати → **звірити РОЗМІР** (час модифікації бреше) → за потреби записати
  вдруге. Траплялося в кожній сесії, де писали багато файлів.

  У 0.9.6 це коштувало окремого циклу й виглядало як фальшивий баг: тест-файл доїхав, а
  `GedcomImporter.cs` відкотився, тож новий тест перевіряв поведінку, якої в скомпільованому коді
  не було, і падіння вказувало не туди. Мораль: **якщо тест падає одразу після запису пачки файлів,
  спершу звірити розміри, а вже потім читати код.** Найдешевше — закривати Visual Studio на час
  запису великих порцій.

## Робочі шляхи

- Рішення: `D:\C#\new projects\FamilyTree\FamilyTree.sln`
- Специфікація: `FamilyTree_Specification.md`
- Журнал змін за версіями: `CHANGELOG.md`
- Майбутні покращення: `IDEAS.md`
- Зразки: `samples/*.familytree`; чужі GEDCOM-файли класти в `samples/gedcom/`
- Обмін GEDCOM: `FamilyTree.Gedcom/` (чернетку `gedcom/` перенесено сюди й видалено)
- Публікація/інсталятор: `FamilyTree.App/Properties/PublishProfiles/win-x64.pubxml`, `installer/`, `docs/build-and-installer.md`

## Як почати новий чат

Дай асистенту цей файл і скажи, що робимо. Типовий порядок:

0. **Зібрати солюшн локально й закомітити.** `dotnet build` + `dotnet test` — це перша реальна
   перевірка `App`, бо пісочниця асистента WPF не збирає. Далі — сім логічних комітів зі списку
   вгорі: у git досі немає нічого з 0.9.6 і 0.9.7.
1. **Ручний прогін того, що зібралося, але в застосунку не бачене.** Тести зелені,
   але жоден із трьох блоків 0.9.6 руками не перевірявся:
   - фото: вибір у редакторі → **Скасувати** (у `%AppData%\FamilyTree\photos` не має лишитися
     файлу-сироти), потім вибір → Зберегти, заміна фото, особа без фото;
   - «Зберегти копію з фото…» — і звірити розмір результату зі звичайним збереженням
     (мініатюри мають бути ТІЛЬКИ в копії);
   - режим «Лише родичі» проти «Усі» на реальному файлі — насамперед свояки й колишнє подружжя;
   - життєві факти: додати особі дві професії й два проживання, зберегти, перевідкрити;
     подивитися сам JSON — `"facts"` має бути лише в тих осіб, у кого факти є, а в решти
     нічого не додалося.

   Плюс увесь 0.9.7, теж не бачений руками:
   - у редакторі особи — **нотатка при народженні, місце й обставини смерті** (два останні
     ховаються разом із датою, коли стоїть галочка «Живий»), у діалозі подружжя — **місце шлюбу**;
   - **зняти «Живий», НЕ вписати дату, зберегти, перевідкрити** — особа має лишитися мертвою
     (це й був сенс `Deceased`); і навпаки — повернути «Живий», зберегти: місце й обставини
     смерті мусять зникнути;
   - **відкрити старий файл (`samples/*.familytree`, вони v2) і зберегти** — це живий прогін
     міграції v2→v3. У збереженому JSON мають бути `"schemaVersion": 3` і `"birth": {…}`,
     а плоских `birthDate`/`birthPlace` — не бути. Дати, місця й нотатки на місці;
   - особа **без** дат і місць: у JSON не має бути ні `"birth"`, ні `"death"` взагалі
     (порожньої події не існує — це інваріант `PersonEvent`);
   - **«Довідка → Про програму»**: два посилання відкриваються в браузері, читаються в
     усіх трьох темах (у фірмовій колір посилання — градієнт) і не ламають ширину вікна
     в англійській мові;
   - **меню на картці дерева** (найбільше ручної перевірки з усього 0.9.7, бо це взаємодія
     мишею): ПКМ по картці відкриває меню з чотирьох пунктів; «Побудувати дерево від особи»
     робить те, що раніше подвійний клік; «Редагувати»/«Додати подружжя»/«Видалити» на
     НЕ-кореневій особі не мають зсувати корінь дерева; підтвердження видалення показує
     кількість зв'язків. Окремо: **панорамування тепер лівою кнопкою** — тягнеться за вільне
     місце, але НЕ за картку (за картку — виділення); відпустити кнопку поза вікном і
     повернутися — полотно не має «липнути» до курсора; у міні-графі «Хто кому» меню по ПКМ
     не з'являється взагалі;
   - **меню на рамці подружжя**: ПКМ по рамці шлюбу (видима смужка навколо й між картками
     пари) відкриває «Редагувати подружжя»; діалог показує саме ЦЮ пару — не виділену особу;
     змінена дата шлюбу одразу видно в тултіпі рамки; корінь дерева не зсувається; позначити
     «розлучені» — рамка має зникнути, а замість неї з'явитися пунктирне ребро. У «Хто кому»
     й на експорті в PNG меню рамки не з'являється;
   - **B-67 (потрібен файл із повторним шлюбом)**: створити пару, одружити (скажімо, 1998),
     розлучити, одружити ЗНОВУ (2012) — у документі буде два `SpouseLink` на ту саму пару.
     Тултіп рамки має показати 2012, меню — відкрити шлюб 2012, а зміна дати в ньому не має
     зачепити запис 1998 (видно в JSON). Заразом перевірити зворотний порядок: спершу чинний
     шлюб у файлі, потім розлучений;
   - **B-68 (той самий файл, крок раніше)**: власне СТВОРЕННЯ другого шлюбу. Розлучити першу
     пару, знявши галочку «В шлюбі» й НЕ вказавши дати розлучення, → «Додати подружжя»:
     колишній має бути в списку без зняття галочки «Приховувати вже пов'язаних», а додавання —
     проходити без «Такий шлюбний зв'язок уже існує». Далі те саме, але без дати нового шлюбу:
     має з'явитися попередження з підтвердженням, а не помилка. І контрольний випадок: поки
     перший шлюб ЧИННИЙ, другий без дат має лишатися помилкою;
   - **B-67, вкладка «Особа» (той самий файл із двома шлюбами)**: у блоці «Подружжя» два рядки
     з періодами в підписі; «Редагувати» на КОЖНОМУ відкриває свій шлюб (перевірити на обох, не
     лише на першому); «Видалити» на одному лишає другий на місці. Для звичайної пари з одним
     шлюбом підпис рядка має лишитися просто іменем, без періоду;
   - **B-69, у ТЕМНІЙ темі**: «Редагувати подружжя» (з дерева й зі списку) — замість списку осіб
     має бути рядок «Шлюб з: Ім'я (роки)», жодної білої плями; у режимі ДОДАВАННЯ подружжя
     список, пошук і фільтри лишаються як були;
   - **B-70 (обов'язково, це була втрата даних)**: файл із повторним шлюбом ЗБЕРЕГТИ, закрити й
     відкрити знову — обидва шлюби мають лишитися, а у звіті про відновлення не має бути
     «дублікатів зв'язків». Перевірити на двох варіантах: без жодних дат (перший `divorced`) і з
     однаковою датою шлюбу, але різними датами розлучення.
2. **Пройти 4c** (Gramps) і закрити T-5.2 — позначити в спеці й оновити цей файл. Чужий `.ged`
   уже імпортовано (0.9.7), лишилася перевірка ЕКСПОРТУ сторонньою програмою — і вона стала
   потрібнішою, ніж була, бо експорт відтоді змінився (`DEAT Y`, `DEAT.PLAC`/`NOTE`, `MARR.PLAC`).
   Заразом покласти той файл у `samples/gedcom/`.
3. **Дописати round-trip тест на експорт життєвих фактів** — єдина дірка в покритті новинки
   0.9.6 (див. «Що лишилось»). Коротко: документ із фактами → експорт → імпорт → факти на місці.
   У 0.9.7 фікстура `GedcomRoundTripTests.BuildFamily` фактів так і не отримала, тож дірка на місці.
4. Далі беклог відкритий. Напрями за спаданням користі:
   - **B-28 + B-20** — обидва про сирі винятки (`ApplyMigrations`, `SettingsService.Save`), тобто та сама
     тема, що й зроблений у 0.9.6 B-09. `ApplyMigrations` після v2→v3 став іще помітнішим:
     міграцій тепер дві, і кидає він голий `InvalidOperationException`;
   - **перф-пачка B-21 + B-27 + лишок B-07** (кеш `FamilyGraph` у «Хто кому», кеш предків кореня,
     LRU-кеш бейджів, кеші `byId`/`childCounts` у `RefreshRelations`) — некритично, але однорідно;
   - **решта 🟠**: B-22 (валідація в редакторі особи), B-23 (`async void OnExit`), B-24 (плейсхолдер
     пошуку), B-26 (експорт PNG), B-29 (`SeedGenerator`);
   - **«Відновити з резервної копії»** (B-12 ч. 3 / S-B8) — нова функція UI, не фікс;
   - з `IDEAS.md` найдешевше й найкорисніше зараз: **пошук по значеннях фактів** («знайти всіх
     ковалів» — дані вже у файлі, а `PersonQuery` їх не бачить) і **масштаб UI** одним множником.
     Повна таблиця стану — у «Що лишилось».

**Що враховувати при плануванні:** `Domain`, `Storage` і `Gedcom` — звичайні `net10.0`-бібліотеки,
і асистент може зібрати й прогнати їх у себе ще до того, як код поїде на машину (рецепт — у
«Важливих нюансах середовища»). Тому задачі в цих трьох проєктах уже не пишуться наосліп;
наосліп лишається тільки `App`.

Асистент працює по одній задачі, з підтвердженням формату/обсягу за потреби, і не починає нову без явного прохання.

**Куди що писати.** Цей файл — стан і плани: що зроблено в двох словах, що лишилось, домовленості
й граблі середовища. Розбір кожної правки («було / стало», файли, тести, ухвалені рішення) —
у `CHANGELOG.md`, розділом тієї версії, у якій вона вийде. Не давати HANDOFF розростатися
в журнал змін удруге.
